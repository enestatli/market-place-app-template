// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCKel4YB0zI8fapF1YQq4On0H5uU5D2exg",
    authDomain: "market-place-app-614b1.firebaseapp.com",
    databaseURL: "https://market-place-app-614b1.firebaseio.com",
    projectId: "market-place-app-614b1",
    storageBucket: "market-place-app-614b1.appspot.com",
    messagingSenderId: "236771815867",
    appId: "1:236771815867:web:cb16fb68351da15aae949e"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
